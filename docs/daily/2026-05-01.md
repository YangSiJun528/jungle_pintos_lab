### User program - 이동석 코치님

초반(Argument passing)이 많이 어려움. 의존성이 큼.

그 뒤에는 시스템 콜 구현하는거, 난이도 다양함.

테스트케이스 많음. 거의 80개, 이해하기 어려운 것도 많음.

팀원들 간의 협업 중요.

개발한 프로젝트 1의 기능에 프로젝트 2가 영향을 받을 수도 있음. (버그만 없으면 어느정도 ㄱㅊ)
동작이 잘 안되는 코드면 사용하지 않는게 좋음.

1주차에 한건, 커널 프로그램 자체의 함수를 실행한 것임. 즉, 테스트도 그냥 함수 호출이지 커널이라는 의미에 맞지 않음.

이제는 유저 프로그램과 같은 걸 실행해서 동작시키는 기능을 만들어야 함.
이제 테스트코드도 유저 레벨의 별도 프로그램으로 관리될 것임.
= 커널의 기능이 작아짐(테스트 로직 제거)

실제로 보면 main 함수가 있음. (테스트가 독립적인 실행파일으로서 존재하게 됨)

프로세스: 실행파일을 실행하는 거.
스레드: 프로세스가 너무 커서 새로 나온 개념, 더 작은 단위로 프로세스를 분리.

멀티스레드ed 프로세스?는 제공하지 않음. 멀티코어 같은거.

멀티 프로세스인데, 여기선 프로세스를 스레드로 구현? 되어있어서 용어를 섞어서 씀.

prcess, systmem-call, exception 쪽만 봐도 충분

나머진 공부하는건 비추 일단 시간이 없으니까. 핵심을 이해해야 함.

파일시스템 부분도 읽을 필요 없음. 프로그램은 파일에 저장되어 있어서 필요한 것.

OS가 파일을 해석하는 방법이 파일시스템

FAT, NTFS, 등등...

근데 우리는 파일시스템 구현 안했음. (프로젝트 4에 FAP 만듦.)

그래서 여기서는 간단하게 더미로 만들어두었음. 그래서 여러 제한이 있음.

이름 길이 제한, 폴더 없고, 트리 아니고, 파일 크기 제한되어있고, 동기화 방법 없음.

-----------

개발환경(맥,윈도우) 테스트코드를 gcc로 컴파일하면,
Pintos 동작하는 x86(인텔의 64bit환경)의 우분투 22.04 에서 실행할 수 있는 프로그램이 나오지 않음.

(여기 놓침)

그래서 해당 환경에 만든 파일을 넘겨지는게 가능해야 함.

그래서 pintos 프로그램 실행할 때, 파일시스템 옵션이나 개발 폴더의 테스트코드 파일 위치 등을 인자로 넘길 수 있게 해줌.

---------------

이제 인자 받고 실행하는거랑, VM나오는데 이거는 다 읽을 필요는 없고,

이제부턴 함수의 동작 flow 같은거, 뭘 수정해야 하는지 AI한테 물어보는게 더 나을 것.

-----

프로세스 생성? 시 load, process_exec 같은 함수를 호출되는데

이 때 인자 같은걸 넘겨줘야 함.

그럴 때, 어떤 식으로 콜스택을 구성해야 하는지에 대한 규칙이 있음.

해당 규칙에 맞게 스택에 넣어줘야 함. 이거를 malloc 구현할 떄처럼 alignment를 해줘야 함.

그래서 어려움.

이것도 리눅스처럼 fork(create_thread) + execute 하는 컨셈임.

----

CPU의 모드 컨셉 설명

범용 목적 커널에선 사용자의 동작을 제어해야 함. 그래서 CPU와 OS에서 모드를 변경하기.

중요한 작업이 필요하면 커널로 가서 모드를 바꾸고 수행해야 함.

이러면 유저 레벨에서 위험한 걸 못하고, 커널만 잘 짜이면 터지지 않게 됨.

syscall로 사용자 모드에서 커널 모드로 이동
iretq로 커널에서 사용자 모드로 이동?

(아주 오래된, or 현재의 작은 cpu에는 이런 기능이 없기도 함.)

커널 모드에선 공유 자원, 보안 관련 동작을 수행

공유 자원은 모든 프로세스가 사용하는 메모리, 키보드, 화면, 저장장치 등임.

printf도 내부적으로 시스템 콜을 호출함. 근데 테스트코드에서 다 printf를 씀.

이러면 printf 디버깅이나 테스트도 하다가 뻗어버림.

그래서 printf기 쓰는 시스템 콜부터 구현하는걸 추천

파일에 읽고 쓰는걸로 화면에 그리고 그런 작업도 수행됨(추상화)

그리고 그 파일 중, 0, 1, 2 파일(fd)는 고정되어있음.

1번이 printf와 같은 곳에서 필요한 일반 출력에 쓰는 것임.

이거부터 해야 함.

-------

VM - 모든 프로세스가 각자 모든 메모리를 쓰는 거라고 생각.

(프로세스 자체가 개별적인 컴퓨터 자원을 쓰는거처럼 한다는 개념을 예정에 특강에서 들었는데 인상깊어서 생각나네)

유저 레벨에서 커널 영억이나 잘못된 위치에 접근하지 못하게 해야 함.   
정해진 위치가 있으므로 이걸 막아야 함.

------

정리

1. OS가 수행하는 유저 프로그램을 실행하는 것이 목표
2. 어떻게 프로그램이 실행되는지 알아야 함
    - 실행파일의 포맷
    - 실행 프로세스(기술적인거 말과 그 과정을 의미함)
3. Pintos에서 모든 기능을 제공하진 않아서 제약이 있음
4. 인자를 스택에 받고 실행하는 규칙을 지켜야 함
5. 시스템 콜도 구현해야 함 - printf를 실행할 수 있는 시스템콜 먼저
    - 나중에 구현하겠지만, fork, exec, wait 이 좀 어려움
        - 구현 시 락이나 세마포 등을 사용해서 동기화가 필요함
6. 5번까지 하고 나면, 테스트 케이스 실행이 안될거임
    - process_wait이 구현이 안되어 있어서 그냥 종료되는데
    - 이러면 다른 스레드 실행하고 OS를 죽여버려서 항상 실패할거임
    - 이거 찾는데 사람들이 많이 해매서 말은 해줌.
7. syscall_handler는 switch가 되었든 뭐가 되었든 나눠서 실행해야 함.
8. 어려워하는 테스트:
    - oom 테스트 케이스
        - 해석을 잘못 해서 그런거임.
        - 이거가지고 4일 밤 새는 경우도 봤는데, 예전 선배 기수에서는 사람들이 잘못 짠 코드를 그대로 써서 문제가 생겼던거임.
    - sync 구현 문제로 인한 실패
        - 스레드 구현, 원본이 잘못되어 있는 경우
        - 될 떄도 있고 안될 때도 있음
        - 대부분 이러면 동기화 구현 문제
9. 추천 순서
    - Argument Passing 빨리
    - 시스템 콜 file 쪽은 디팬던시 없는 거 끼리 나눠서 처리
    - 테스트코드가 한 기능에 대한 여러 엣지 케이스가 있어서 AI한테 부탁해서 작업 분담하는게 좋을듯

----

extra 과제

dup2 구현하는건데, 기존 코드가 다 망가지게 될 수 있어서 주의하기

꼭 할 필요는 없음. 2주동안 했을 때도 다 못한 팀이 있었음

---

(근데 코치님 SingleFile 익스텐션 쓰네 저거 좋긴함)

### 공부하기 계획

일단 자료들 대충 읽고, 유튜브 스크립트랑 같이 읽으면서 2~3시간 정도 큰 그림 잡는데 쓰고

Argument Passing 이해하고 구현하는거에 집중하기

### 전체 공부 메모

일단 유튜브 내용이 자세하긴 한데, 강의의 내용과는 다르다. 코스 이름이 다르고 32bit를 기반으로 함.

그래서 지금부터는 참고용으로만 쓰고, 실제 구현에는 차이가 클거 같아서 볼 수 없다.

난이도가 올라버림;;

---

일단, 커널이 프로그램 시작을 위해서 CPU가 이해하는 형태로 첫 call stack을 구축하는 것이 첫 목표인 것 같다.

또한 이 과정에서 전달되는 argc, argv를 저장하기도 해야 한다.

일단 위 작업은 다 load에서 수행하는거 같고, file_name은 로드 작업 후에 free 됨.
load에서 파일 열고 ELF 포맷 읽고 그런거 다 하는듯?

그래서 첫 스택프레임 구현과 파싱만 하면 될거 같은데.

근데 이런거 파싱하고 했으면 결과를 어딘가에 저장해두고 메모리 상으로 살려줘야 하는데,   
그게 그냥 stack 메모리 영역에 두니까 괜찮은건가? 변수로 저장해두는게 아니니까?

그러면 memcpy같은거 쓸려나.


`load()` -> `do_iret()` 순인데, 역할이나 분석 결과로 보면 - (009, 001 문서 참고)

`do_iret()`는 `intr_frame` 상태를 레지스터로 복구하고 해당 thread를 실행하게 됨.

그러면 `load()`에서는 `intr_frame`을 수정해서 적절하게 값을 쓰기만 하면 될 거 같음.
 
----

회의다녀왔는데 argument passing이 내 업무가 아니네;;; AI가 나눌 때 정현님 파트였음.

근데 나는 내가 다 구현하거나 하는게 목표고, 내가 더 빠를수도 있으니까 내가 하긴 하는데, 정현님도 함.

아마 팀 프로젝트에 반영하는건 정현님이 하지 않을까.

근데 회의에서 문자열 처리하는 유틸인 `strtok_r()`(`string.h` 에 있음)이 있다고 하네, 이러면 훨씬 쉽지.

바로 구현 들어가면 될 듯?

분석하면서 빠르게 이해하기 위해서 009~011 문서 작성

의존도가 좀 높아진거 같은데 어쩔수 없긴 함. 시간이 없으니...

그리고 분석 결과 가져다 쓰는거 정도면  주도권을 내가 가지고 있는 거기도 하고.

근데 이거 단어 인자를 어떻게 구하지?
흠...

file_name인자에 어떤 값이 들어가는지 보고 싶은데, 이거 실행이 안되는게...   
아직 시스템콜이나 그런게 하나도 구현이 안되서 안될거 같은데

이거는 검증을 못할거 같으니 AI한테 물어봄.

file_name이 사실 상 command line이라고 함.

아하 내용이 있었네

> 현재 process_exec()는 새 process(프로세스)에 argument를 전달하는 기능을 지원하지 않습니다. process_exec()를 확장하여 이 기능을 구현하세요. 단순히 program file name(프로그램 파일 이름)을 argument로 받는 대신, space(공백)에서 단어로 나누도록 만듭니다. 첫 번째 word는 program name이고, 두 번째 word는 첫 번째 argument이며, 그 뒤도 마찬가지입니다. 즉 process_exec("grep foo bar")는 grep을 실행하면서 두 argument foo와 bar를 전달해야 합니다.    
> command line(명령줄) 안에서 여러 space는 하나의 space와 같으므로 process_exec("grep foo bar")는 원래 예제와 equivalent(동등)합니다. command line argument 길이에 reasonable limit(합리적인 제한)을 둘 수 있습니다. 예를 들어 argument가 single page(단일 페이지, 4 kB)에 들어가는 범위로 제한할 수 있습니다. pintos utility(유틸리티)가 kernel에 전달할 수 있는 command-line argument에는 이와 별개로 128 bytes 제한이 있습니다.

그러면 file name 잘 넘겨주게 하고.

load는 static이고 건드리는 부분 딱히 없으니까 위에서 쪼개서 값 인자 받는거 시그니처 바꿔서 보내줘도 될 듯?


argv를 저장하려면 문자열을 저장하는 2차원 배열을 만들어야 하는데...

총 크기 버퍼 사이즈가 있고 그걸 나눠서 써야하는데 흠...

AI한테 기법 물어보니까 코드 나옴????

씁 근데 이정도는 괜찮을지도, 핵심 구현보다는 예시로 든거니까. 좀 넒게 보면 범용 코드일지도.

근데 이해 안가서 자료 찾아봄 - https://wikidocs.net/86260

아마 토크나이저는 file_name의 특정 부분은 포인터로 들고 있는거 같음. 그래서 그냥 char * 배열로
포인터만 들고있어도 되는 듯? 인자 개수 제한은 없었나, 대충 정함.

-----

근데 이거 주소 세팅하려면 결국에 ifram의 rsp 저장된 위치를 알아야 하는데

이게 지금 스택 위치는 아닐꺼란말이지? (이미 쓰이고 있으니까)

그럼 그걸 어디에 저장하는거지 - 이건 AI 활용

아 이미 새로 분기가 된 상태니까 비어있는게 맞네, 그냥 덮어씌우면 됨.

----


일단 대충 구현은 한거 같은데 테스트 다 실패함.

기대도 안했음

어디가 문제일까...

`Run didn't produce any output` << 이거 에러남.

아 근데 테스트하려면 뭐 출력하는거 만들어야한다하지 않았나???

----

유저 레벨에서 뭔가 찍는게 안되는거고, 커널에서 printf 하는건 될꺼같은데

`hex_dump()` 같은거 출력하게 하면 괜찮을거 같은데 일단 바로 종료되는거부터 막기

일단 `process_wait()`이 문제고, 이거는 무한루프 돌리면 될 듯? 귀찮으니까 이거 만든 팀한테 코드 보여달라해서 참고하기.

보니까 큰 수 주고 while로 줄이면서 시간 지나면 끝나게 함. 
괜찮은데? 테스트 오래걸리는거만 아니면 명시적으로 끄는거보다 나은 듯?

이거 AI가 `...Debug는 .output 확인이나 hex_dump() 쪽으로 보는 게 낫습니다.`래서 물어보니까.

`.output`은 커널이 실행한 printf 결과가 들어간 곳이라 지금 찍은게 보이고

`hex_dump()`는 디버깅용으로 stack 메모리를 읽기 쉽게 출력하는거, 이거 써보면 될듯?

이거는 AI한테 사용 예시 물어봐서 처리하기. 뭐 핵심 기능 아니니까.

근데 출력이 안되는데? 흠... 무한 대기로 바꿔볼까

assert 에러남 뭔가 구현이 잘못되었나보네, wait 시간도 더 늘리거나 일단 무한 대기 쓰거나 해야할 듯?

-----

일단 파싱 에러나는건 해결된듯?

load에서 에러나네 

아니 근데

"Kernel panic in run: PANIC at ../../threads/thread.c:398 in thread_current(): assertion `is_thread (t)' failed."
???

이게 왜 스레드가 아니야 해당 파트는 별 말 없었는데 왜 실패하는거지, 이거 수정하라는 말도 없었는데?

AI한테 힌트 받기 감도 안오네

파싱할때 지역변수 쓴게 스택 포인터 위치를 덮어씌웠다는데... 유저 커널 위치야 아직 아닐거고.

현재 커널 위치를 지역변수가 덮어 씌울 수 있나?

------

> Project 1 introduction에 “각 thread의 kernel stack은 4KB보다 작고, 큰 local array를 두면 이상한 panic이
날 수 있다. 대안은 page allocator/block allocator”라고 되어 있습니다: docs/reference/pintos-kaist-
original/1_project1/0_introduction.md:21

>   그리고 skeleton 코드 자체도 이미 이 패턴을 씁니다.
> 
> - command line 복사용 page: pintos/userprog/process.c:58
> - ELF segment/user stack page 할당: pintos/userprog/process.c:710, setup_stack()

일단 버퍼 크기를 낮추고 문제 넘어가는지 확인 

1kb 이하로 쓰게하기. 하긴 로컬 스택인데 argument 받는 배열 버퍼를 4kb로 설정했네
생각이 없었다.

일단 넘어가지는거 확인. 이러면 메모리 블록 할당하게 쓰기

------------

메모리 블록을 쓰는데, argv 마지막 부분을 null로 하는법을 모르겠음.

시간 좀 썼는데도 모르겠어서 AI 도움 받음. 순수하게 문법적인 부분에서 모르겠어;;

------------

근데 돌려보니까 패닉나긴 하는데, 이거 아마 할당 메모리 다른 곳 보고있나본데 뭐 암튼 그거랑 별개로

루프를 안도네

아 처음에 한번 이미 더 호출하는 코드가 있었네 꼬인듯?

-------

루프 잘 도는거까진 확인, 반복하면서 인자 받는 패턴이 잘못된거 같은데

이런저런 시도해보고 안되면 hexdump 찍기

이거 테스트 파일 써진가 반영 잘 안됨 

답답해서 단축키 외움. `opt + cmd + y` 쓰면 폴더 리로드함

-------

hex dump 보면 잘 세팅된거 같은데 흠...

왜 접근하고 출력하려고 하자마자 죽지? 이거는 힌트 받아야겠다 모르겠음.

-----

Page-Fault Exception이 나는 이유를 물어봄.

심지어 이게 이미 할당받은 공간에 접근하는거 같은데 왜 에러가 나는가...

아 이게 char ** 배열은 8 byte(cpu아키텍처가 64니까) 단위로 보는데

실제로는 그냥 \0으로 구분되어있는거니까? 그러면 오프셋 더해가면서 읽어야하네

이렇게 바꾸니까 일단 파싱까지는 해결 

load에서 깨지기 시작함.

----

이거 파싱된거 읽는 방법 문제임. 아까 했던 방식대로 수정중...

그리고 나중에 argv 라고 이름 쓴거 바꿔야하나? 좀 햇갈리는데

일단 실패함.

기존 printf 내역이 복잡해서 이거 좀 줄여야 할 듯?

일단 커밋해

----

엥 근데 지역변수 크기 줄였는데도 에러나네, 아무데나 `thread_current ();` 찍어서 확인해보기

똑같이 load 호출 될 때만 에러나네 load 바깥에서는 문제 없고,

무슨 차이가 있는거지? 그냥 함수 호출하겠다도 콜스택 하나 쌓이는거 말고 내부적인 차이가 없을거 같은데

이거는 AI한테 힌트 받기

아 선언이 위에 없어서 못봤네, 인자 크기가 너무 커서 그런듯?
콜스택 생길 때부터 그런거니까. 

고수준 언어만 써서 그런가 이런 상황이 익숙치가 않음;;

그러면 흠... 일단 임의로 32개 정도로 줄이고
나중에 할당하던가 하기...

선언을 위에 올려놔야 하나? 오히려 이게 저수준 다룰라면 직관적인거 같기도 하고...

-----

이거 hexdump 스택 부분 찍고 싶은데 자꾸 에러나서 걍 포기함.

AI한테 힌트 달라했는데 그래도 모르곘음...

그냥 구현해달라하기 진짜 모르겠음. 30분 거의 잡고있는거같은데

테스트 못하니까 그냥 테스트코드 대체한다 생각하고... 알려달라하기

근데 그거 찍으니까 안게 이거 rsp에 쓸 때 값을 덮어씌워버림

포인터 연산을 했어야 했는데...

이해 안가서 ai한테 문서 요청함.

-----

일단 쓰면서 print 찍어서 스택 잘 그려지는지 보는 중.

에러는 free 쪽에서 나는데 일단 무시하고 버그 수정 중...

```
000000004747ffc0  00 00 00 00 00 00 00 00-00 00 00 00 00 00 00 00 |................|
000000004747ffd0  ed ff 47 47 00 00 00 00-f9 ff 47 47 00 00 00 00 |..GG......GG....|
000000004747ffe0  00 00 00 00 00 00 00 00-00 00 00 00 00 61 72 67 |.............arg|
000000004747fff0  73 2d 73 69 6e 67 6c 65-00 6f 6e 65 61 72 67 00 |s-single.onearg.| 
```

일단 잘 찍히고 리틀 엔디안이라 그런거지 주소 잘 찍는거 같은데?

시작 주소 옆에 나오는데 이상하긴 한데, 1195900904가 8 배수로 얼라인된 부분임.

이거 AI한테 분석 맡기기

```
0x4747ffc0  0x0000000000000000    // 뭔가 추가로 들어간 0
0x4747ffc8  0x0000000000000000    // fake return address 가능성 높음
0x4747ffd0  0x000000004747ffed    // argv[0]
0x4747ffd8  0x000000004747fff9    // argv[1]
0x4747ffe0  0x0000000000000000    // argv[2] = NULL
0x4747ffe8  padding
0x4747ffed  "args-single\0"
0x4747fff9  "onearg\0"
```

잘 들어간거 맞는듯?

이러면 free 부분 버그 수정하고 손보고나서 PR?

----

filename을 풀고 한번 더 해버려서 꼬인거였음. 

에러 안나고 딱히 문제 없는 듯? 다듬고 작업 옮기기 ㄱㄱ

----
