### Supplemental Page Table - Revisit 파트 하기

spt copy랑 kill을 해야 함.

이거 fork 문제로 걱정해서 미뤘는데, 지금은 알게 됨.

COW를 구현하지 않는 이상, 그냥 frame까지 복사하면 됨.

그럼 frame 내부 내용은 memcpy로 복사하면 되려나? 그러면 비효율이 확실히 커지긴 하겠네...

`pte_duplicate` 인가 그거 참고하면 될 듯? (frame이랑 page 써야하니 동일하진 않을듯?)

----

근데 주석에서 보면 `vm_alloc_page()`를 써서 바로 매핑(폴트 없이) 가능하다는데

이게 어떻게 되는거지? init이 없으면 바로 하나? 내부 코드를 뒤져봐도 명확하게 보이는게 없는데?

직접 claim해서 OK 해줘야 함. (아 알고 있었는데 왜 이러냐...) 

### Page Cleanup 파트 하기