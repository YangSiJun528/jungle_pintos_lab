### 마지막으로 VM 개념 정리 후 다른 구현 찾기

`KERN_BASE` 까지 메모리 공간을 쓸 수 있는건데, 
너무 적지 않나 싶었는데, Pintos만 해도 유저 공간이 512GB라서 넉넉하다고 함.

일단, 018, 019(그림이라 일치 안할 수 있어서 이해용)을 위주로 하고,

나머지는 참고용으로 보는게 맞을 듯? 이해도가 좀 낮을 때 쓴 거라

### 코어타임

코드리뷰하기

시원님 작업은 AI 쓴게 많이 보이긴 하는데 시간 관계 상 어쩔 수 없었던 듯?

그거 기반으로 내용 공부하고 내 식으로 옮겨보든가 하기

그리고 남는 시스템 콜 구현 들어가기

### exec/fork/wait 내용 이해하기 

#### 시작

일단 보니까 fork 하면 parent랑 child랑 fd나 뭐 그런 데이터를 공유하는 듯?

그리고 병렬적인 스레드가 생기는거라 parent 스레드에서 초기화 후 child에서 작업을 진행하는 식으로 되어야 함.

이를 위해서 앞서 말한 parent랑 child 공유에서 `parent 스레드에서 child 설정 중`, `child에서 설정 작업 끝남`

의 실행 흐름을 제어하기 위해서 2개의 세마포어가 필요한 듯 함.

그리고 이런 커널 영역의 락을 쥐고 있는 상태라서, 
예외상황(유효하지 않은 메모리 접근 등) 시 free 해주는 cleanup 구현도 필요하고

일단 요구사항부터 구체적으로 찾아봐야 할 거 같음. 지금 코드가 잘 이해되는 상태는 아니라서

#### 요구사항 분석 AI 돌리기

### 이거 추천한 작업 순서대로 작업 하되, 팀원들과 작업 싱크 맞춰야 하니까 그거 우선해서 하기

일단 구현 우선순위 AI한테 레퍼런스 기반으로 요청함.

그리고 기존 팀원들의 구현을 참고하여 구현하기.

시간 관계 상, 그리고 리뷰하면서 이미 알게 된 내용을 기반으로 하는게 나음.

어쩔 수 없기도 하고, 대신 내 스타일 바꿔서 쓸 듯?

020 번 문서에 정리

### 2. process 종료 상태와 공통 종료 경로

exit(status) 함수를 만들고, 스레드에 해당 상태를 적게 하기

근데 하다보니까 이거저거 하게 됨. 

프로세스 init 시 프로세스 이름만 들어가게하고,

페이지 next 처리하는거 추가하고,

handle_write, handle_exit 추가하고... 함

이러니까 006-010 페이지 테스트 통과

### 

### 번외: 인자를 처리하는데 쓰이는 스택 오버플로우를 고려해야 하는가?

이거 다른 팀 코드 보다가 말한건데, 지금 보면 initd나 이런 부분에서부터 다 확인된다는 말이지? 

근데 그럼 굳이 해야할까 싶기도 하고, 그리고 스택 공간 4kb 고정인데, 이거 넘어가면 그냥 바로 커널에서 실패하는게 맞지 않나...

그럼 예외처리가 필요하긴 한데, 그게 없으니까, 근데 뭐 굳이? 문제가 있긴 한데, 이거까지 처리해줘야 하나...

toy니까 우선순위가 낮은듯?

리눅스에서는 이런 식으로 제한이 있음. 

https://trofi.github.io/posts/299-maximum-argument-count-on-linux-and-in-gcc.html
