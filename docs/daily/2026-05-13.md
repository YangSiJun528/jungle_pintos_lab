### project 2 테스트 통과하게 하기

실패하는 것들 목록

read/write 쪽이 잘못되어있을 가능성이 있을듯? 이거랑 관련한 메모리?

아니면 아마 파일 읽는 그런거 문제가 있으려냐? 일단 뒤지는거 위에는 방법이 없음.

``` 
FAIL tests/userprog/read-boundary
FAIL tests/userprog/write-normal
FAIL tests/userprog/fork-read
FAIL tests/userprog/fork-close
FAIL tests/userprog/exec-once
FAIL tests/userprog/exec-arg
FAIL tests/userprog/exec-boundary
FAIL tests/userprog/exec-read
FAIL tests/userprog/wait-simple
FAIL tests/userprog/wait-twice
FAIL tests/userprog/wait-killed
FAIL tests/userprog/multi-recurse
FAIL tests/userprog/multi-child-fd
FAIL tests/userprog/rox-child
FAIL tests/userprog/rox-multichild
FAIL tests/filesys/base/syn-read
FAIL tests/filesys/base/syn-write
```

### 그 전에 로깅하고 분석하는법 자료 만들어서 공유하기

지금 그나마 6조만 로깅 좀 하고 (페어로 해서 진혁/채강님이 사용하는 듯?)

우리 조도 나만 좀 하는 편이라 이걸 자료로 만들어서 공개하면 좋지 않을까...

인터넷에 있는 좋은 자료가 있는지? 지금 로깅 패턴이랑 비슷하면서 그거 기반으로 버그 찾는 예시 소개하는거
있으면 공유하고

그리고 그거 기반으로 핀토스에서 나는 어떤 식으로 했는지 어제 내용 기반으로 추가 자료 정리하고

이거는 AI 좀 쓰기. 코딩하는거랑 아예 별개의 활동이니까.

그리고 스타일 별로니까 design.md 좀 쓰고?

병렬로 하면서 032번 문서로 생성함.

https://html-preview.github.io/?url=https://github.com/YangSiJun528/jungle_pintos_lab/blob/main/docs/ai/032_%EB%94%94%EB%B2%84%EA%B9%85%EC%9D%84_%EC%9C%84%ED%95%9C_%EB%A1%9C%EA%B9%85_%EB%B0%9C%ED%91%9C.html

### 다시 테스트 작업 진행

#### fork read/close 쪽 보기

발표 자료랑 병럴적으로 할 듯?

```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" \
  build/tests/userprog/fork-once.result \
  build/tests/userprog/fork-read.result  \
  build/tests/userprog/fork-close.result 
```


테스트 주석 보기

`fork-read` 

> 포크 후 자식 프로세스는 열린 파일을 읽고 닫고, 부모 프로세스는 닫힌 파일에 접근합니다.

`fork-close`

> 포크 후 자식 프로세스는 열린 파일을 닫고, 부모 프로세스는 닫힌 파일에 접근합니다.

close가 문제인 듯? 일단 read는 close가 해결되어야 다른게 문제인지 알거 같은데

일단 닫는거 자체가 공통 동작이라서?

자식에서 닫은걸 부모에서 접근하면 문제가 되는가?

근데 이거 복사해서 문제 없지 않나? 어제 이거 문제 이미 생각해서 수정했던거 같은데

둘 다 뭔가 init에서 실패해서 패닉 뜨는듯? 

로그 찍어서 어디서 실패하는지 보기

```
- (fork-close) Parent success
- (fork-close) end
- fork-close: exit(0) 
...
+ fork-close: exit(-1)
```

이거 문제네

왜 실패하는걸까?

자식이 끝나고 다시 읽는데, 이 자원이 공유되는건가?

그러면 흠... 

일단 projects 2에선 문제가 없었으니

또 spt kill까지 문제 없이 되는거 보면 뭔가 복사가 잘못된건 맞는듯?

근데 메모리 영역 복사하는거에서 파일 객체까지 다 복사할 수 있나? 지금 보면  
그리고 그게 `supplemental_page_table_copy`의 책임인가?

Open 된 파일을 자식이랑 부모랑 공유해서 offset이 잘못되든 파일을 수정하든 한거 같은데

아니다 다시 보니까
```
부모 -> 자식 생성 -> 대기 -> 파일 읽기  
자식 -> 파일 종료 -> 자식 종료
```

순이라 동일한 파일을 메모리에 들고 있는게 문제인가?

근데 이게 fork의 문제지 vm의 문제는 아니지 않나??

이거 실행해서 확인 중...

```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" \
  build/tests/userprog/fork-once.result \
  build/tests/userprog/fork-read.result  \
  build/tests/userprog/fork-close.result 
```

이러면 또 통과하네?

그러면 fork할 때 vm 복사하는 쪽에서 파일 복사가 빠진듯?

fd 복사는 이미 하고 있는데? 

```
#ifdef VM
	supplemental_page_table_init (&current->spt);
	if (!supplemental_page_table_copy (&current->spt, &parent->spt)) {
		goto error;
	}
#else
	if (!pml4_for_each (parent->pml4, duplicate_pte, parent)) {
		goto error;
	}
#endif
```

여기 아니면 다른 부분이 없음. 

syscall 쪽에서 file 상태 까보게 로그 찍어보기, 여기가 문제같은데?

아니면 file의 포인터 주소를 잘못 주거나? << 오 이건가?

근데 그렇다기엔 그냥 dup 함수 호출해서 file 복사하는거라 상관 없을거 같긴 한데...

sys_cl 이 1번만 호출되네, 그러면 wait이 문제인가?

동시성 생각하면 그럴지도?

#### 근본 원인이 exec인거 같음 이거 먼저 수정

보니까 새 tid 4를 load 할 때 에러가 나서 중단되는거 같음.

이거 먼저 찾아야 함.

`exec-once` 테스트 돌리기

아니 근데 이거 print 문 내용 진짜에요? 

개뜬금없네

```
void
test_main (void) 
{
  msg ("I'm your father");
  exec ("child-simple");
}
```

`page found in vm_alloc_init` 발생

어디선가 이걸 초기화를 미리 해주나? 로그 뒤지면 나올듯?

다른 팀 이야기하는거 들었어가지고 물어보니까 의도치 않게 원인을 알게 됨.

exec 할 때 init을 안해줘서 이렇게 될 수 있다고 함.

추가해보니까 잘 됨.

#### printf 로 인한 문제 고민

그리고 또 문제가, print 때문에 실패할때가 종종 있더라; 이거는 해결방법을 생각해봐야겠음.

확실한 부분은 인터럽트처럼 좀 함수 앞 뒤로 변수 값 바꿔서 활성화 비활성화 하게 하거나 해야할 듯?

#### 암튼 다시 check

```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" check
```

실패한것들

```
FAIL tests/userprog/read-boundary
FAIL tests/userprog/write-normal
FAIL tests/userprog/fork-read
FAIL tests/userprog/fork-close
FAIL tests/userprog/exec-read
FAIL tests/userprog/rox-multichild
FAIL tests/filesys/base/syn-write
FAIL tests/vm/cow/cow-simple
```

좀 많이 줄긴 했네

#### 이러면 read나 write 보기

exec-read가 fork 보다 더 우선되어야 하는거 같음.

근데 이거는 입출력 쪽 문제인가? 라기에는 아까처럼 문제있는게 확률이 높으니까...

read-boundary이나 write-normal을 먼저 봐야하나?

일단 여기서 커밋 (exec 약간 해결)

```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" \
  build/tests/userprog/write-normal.result \
  build/tests/userprog/read-boundary.result  
```

일단 sys call 에서 write이 문제인건 맞네


```
[DEG] r=thrd tid= 3 p4=0x800424a000 k=sys.wr fn=syscall_hand vals={fd=1, buf=0x4060e0 sz=21}
(write-normal) begin
[DEG] r=thrd tid= 3 p4=0x800424a000 k=sys.wr fn=syscall_hand vals={fd=1, buf=0x4060e0 sz=33}
(write-normal) create "test.txt"
[DEG] r=thrd tid= 3 p4=0x800424a000 k=sys.wr fn=syscall_hand vals={fd=1, buf=0x4060e0 sz=31}
(write-normal) open "test.txt"
[DEG] r=thrd tid= 3 p4=0x800424a000 k=sys.wr fn=syscall_hand vals={fd=2, buf=0x405f40 sz=373}
write-normal: exit(-1)
[DEG] r=thrd tid= 3 p4=0x800424a000 k=call   fn=supplemental args={kill spt=0x80042490f0} 
```

콘솔 출력은 문제 없음. 파일 쓰기 시 문제

이유를 확인중...

아 유효성 검사할 때 pml4가 아니라 spt를 봐야하는구나?

오 이러니까 둘 다 통과함. 다시 체크 ㄱㄱ 다른것도 이거 영향받는거 좀 있을듯?


```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" check
```

실패하는 테스트

레전드

거의 다 줄었는데? 근데 이건 동시성 이슈인건가?

```
FAIL tests/userprog/multi-recurse
FAIL tests/userprog/rox-multichild
```

일단 여기서 커밋

#### 중간에 드는 로깅의 중요성

6조도 잘 되는 하는데, 전체 로깅을 안하니까 다른 부분에서 가정하거나

전체 흐름을 다 흝어봐야 해서 오래 걸리고 있음.

나는 로깅 다 찍어두니까 영역 제한이 상대적으로 쉬웠던 거 같음.

#### 다시 실패하는 2개 원인 파악

-----

이건 왜 실패하는가...

```
make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" \
  build/tests/userprog/multi-recurse.result \
  build/tests/userprog/rox-multichild.result 
```

multi-recurse 통과하네? 동시성 이슈가 있나?

여러번 실행해보기
```
make -C "$PINTOS_ROOT/vm" clean build/tests/userprog/multi-recurse.result
```

5번 했는데 다 통과함. 좀 간헐적인 이슈인가? 이번에 운이 안 좋았던거고?

단일로 돌려서 그런가? check를 돌린걸 보면 다 실패했다고 뜨긴 함. 다시 check 한번 ㄱㄱ

이러니까 또 성공하네? 뭐 아까 잠깐 수정했던걸로 해결되었거나 아니면 희귀한 동시성 이슈? 일수도?

#### 일단 넘기고 rox를 보기

아마 fork 할 때 쫌 문제가 있는 듯???

rox는 실행파일 못 쓰게? 하는거니까 그걸 ref cnt 기반으로 안해서 그런가? 
close 할 때마다 뒤져봐야 하나?

```
make -C "$PINTOS_ROOT/vm" clean 
make -C "$PINTOS_ROOT/vm" \
  build/tests/userprog/rox-child.result \
  build/tests/userprog/rox-multichild.result   
```

주석 보면 자식 프로세스에 넘길 때 쓸 수 없는 상태인거 복사가 안된건가?

```
/* Ensure that the executable of a running process cannot be
   modified, even in the presence of multiple children. */ 
```

이것도 이전 공유 자원과 비슷한 문제 같은데? 이전거에 비해서 좀 오래걸릴거 같아서 002에 저장

이거 ref 기반으로 해야하나? 아니 근데 파일 자체는 괜찮지 않나?

아니 근데 로그 보니까 마지막에 실패하는데 실행 횟수와 연관이 있나?

테스트 코드를 보면 반복 횟수 1이 child고, 
5가 multichild인데 이걸 4, 2 까지 줄여봄.

그래도 실패함. 뭔가 공유 자원에 운제가 있나?

rox-child.inc 코드를 뒤져봄. 이거 뭔 확장자임 근데

일단 반복해서 쓰는거까지는 되는데, 여러번 실행하면 이 테스트 결과 code가 0이 아닌거 같음.

그리고 특정 상황에서 잘못된 접근으로 패닉이 나는거 같고.

로그를 찍을 때 에러가 나니까 원인을 알 수가 없네;;;

exit(12)는 무슨 상황에서 나는걸까? 아니네 의도적인거 같은데?

어느 라인에서 에러가 나느넌지 모르겠네

그니까 음... 아직 오픈 한게 살아있는데 그 때 쓰려니까 실패한거 아닌가?

뭐 출력 쪽도 문제 없는데

이걸 어떤 기준으로 FAILED로 출력하는거지? 이걸 하는 코드도 안보임.

뭐 더 상위에서 처ㄹ리하는건가? 

테스트 코드 뜯어보는건 좀 아닌거 같은데 이거 AI 쓸까 말까

가능하면 지금 아예 안쓰고 있어서 안쓰고 싶긴 한데 아무리 생각해도 삽질같단 말이지?

고민 중...

아 이게 매크로라 여기서 fail을 쓰네

```
  /* child-rox에 다시 쓴다. */
  seek (handle, 0);
  CHECK (write (handle, buffer, sizeof buffer) == (int) sizeof buffer,
         "write \"child-rox\"");
} 
```

다행히 안쓰고 찾음.

이거 조건을 왜 만족하지 않을까?

일단 로그 보면 에러 안난거 같은데 확실하진 않으니 이거 기준으로 확인 ㄱㄱ 

----------------

CHILD_CNT가 1은 통과하는데 2는 실패

뭔가 읽고 쓰는게 문제가 되는건 확실함.

이거 결과가 0인걸 보면 파일에 접근을 못하거나 읽는게 불가능하다?

```
SYS_SEEK fd=2, file=0x8004244048 position=0
(rox-multichild) ----rs 0  sizeof buffer={16} 
```

저게 0을 반환하는 기준이 뭐지?

더 찍어보니까 inode_write_at까지 들어가서 뭔가 닫힌 걸 쓰는거 같은데?

닫힌 상태인지 아닌지 어떻게 알지? 

cleanup이 문제인가? 여기서 pml4 하는데 여기서 뭐 이상한거 하나? 딱히 없는거 같은데? 

아 이거 process_exit이 문제네 이거 ref cnt 기반으로 하는거 맞는듯?

이거 아니면 아닐 이유가 없음.

하... 내꺼 기존 코드 참고해서 수정하고 다시 테스트 ㄱㄱ

근데 이거 이전에도 실패했나??? 모르겠다.

#### ref cnt 기반으로 rox 수정하기

아닌가? 잘못본거같기도 이미 되어있었는데?

그럼 왜 0인거야?

기존거 잘 되는지 확인

```
export PINTOS_ROOT=/workspace/pintos
source "$PINTOS_ROOT/activate"

make -C "$PINTOS_ROOT/userprog" clean
make -C "$PINTOS_ROOT/userprog" \
  build/tests/userprog/rox-child.result \
  build/tests/userprog/rox-multichild.result   
```

이번엔 현상은 진짜 찐임 003 폴더에 저장

보니까 `inode->deny_write_cnt=0`가 0임.

그래서 0을 반환하는거 같은데 이게 왜 0인가? 그런걸 좀 찾아봐야 할 듯?

일단 근본 원인은 몰라도 이유는 알았음.

하.........

쉽지않다 정말

