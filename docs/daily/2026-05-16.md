### mmap 테스트 틀린거 보고 고치기

일단 상위 3개만,

`mmap-read`
`mmap-close`
`mmap-unmap`


`mmap-read`는 성공

기본적인 구현도 잘못되었을 듯?

```
make -C "$PINTOS_ROOT/vm" clean
make -C "$PINTOS_ROOT/vm" \
  build/tests/vm/mmap-read.result \
  build/tests/vm/mmap-close.result \
  build/tests/vm/mmap-unmap.result 
```

보니까  

`FAIL in file_read` 패닉 남.

파일이 없거나 그럴지도? << 그건 아님. 그랬으면 이미 실패해야 함.

더 추가해야할수도 있을거 같은데 호출 횟수가 많은 부분이라 어떻게해야할지 잘 모르겠네

이거 로그 안찍으면 통과하는 영역이 있고, 찍으면 어디선가에서 실패함.

뭐가 문제일까...

이전에도 이런 문제가 있었는데...

일단 로그 끄면 잘 되긴 함. 프린트 찍어도 잘 되고

그러면 터지는 위치의 포린터 값이 잘못되어 터지는걸지도?

왜 근데 스레드 관련 에러가 나는거지? 흠...


----

보니까 중복 vm_claim이 문제인듯?

근데 이거 할당 처리를 할라면 실제로 접근을 해애햐나 그럼???

보니까 출력에서 에러나는게 bytes_read << 이거 문제임.

로직 문제는 아닐수도 있을거 같은데 롤백해야하나?

아니 근데 저거 int 타입 아닌가? 왜 에러나는거지?

그냥 변수를 출력하는거 자체가 버그인건가????

코드 없이 AI 챗봇으로 질문 ㄱㄱ

1시간 지났으

분기문으로 닫고 클레임 안된걸 호출할라하니까 문제였던거 같음. 

그럼 그냥 자연스럽게 페이지에 로그되는걸 원하는데, 이걸 어떻게 해애야하지?

그냥 포인터로 접근해버리면 되나?

일단 롤백하고 다시 ㄱㄱ

### 계속 원인을 알 수 없는 곳에서의 에러가 발생함...

그냥 이거 롤백하고 아예 처음부터 출력 하나씩 늘려가면서 돌릴 듯?

핵심 개념은 알고 있고, git 로그 보면서 추가하면 되니까.

일단 mmap 부터 순차적으로 해서 최소한으로 통과하면 추가하는 식으로 해야 할 듯??

지금 어디서부터 문제인지 감도 안 잡힘.

### 다시 ㄱㄱ

이러니까 로그 찍히면서 실패함.

상황은 더 나아짐.

보니까 length가 유저가 넘겨준걸 하는게 문제였음.

zero byte 예상치랑 달라서 그런거, 실제 파일 크기만큼 zero_bytes를 설정해야 함.

근데 파일을 읽는건데 파일 크기보다 작은 length가 있으면 어케되는거지?

머리가 안돌아가네 러닝하고 와서 밥먹고 화이트보드에 좀 그려가면서 해야할 듯?

-----

이거 러닝하러 가면서 mmap 구현하는 다른 팀들하고 이야기하니까 
대충 유저의 length가 더 큰 경우는 결론 나와서 하니까 해결됨.

그러면 이제 일단 munmap까지 ㄱㄱ

이거 뭐 종료 시에 문제 생김.

로그로는 맞는거 같은데 아마 destory 하는 쪽이 문제인가?

아 이거 destroy 이후에 접근해서 그런 듯? 수정하니까 패닉은 나긴 한데 언맵은 성공함.

나중에 spt에서 찾으려는데 없는듯?

파일을 닫아서 문제인건가?

근데 reopen으로 넘겨주는데? 근데 이거 duplicate랑 차이가 뭐지

엥 file_reopen 안하네? 롤백하면서 지워졌나?? 

찾아보니까 대체? 에 가깝다는데 "기존 파일 객체를 다른 파일/스트림으로 갈아끼우는 것"

이게 아니지 않나? 그러면 file_reopen은 왜 써야하는거지?

이런거 모르면 계속 삽질하게 될 수 있어서 좀 찝찝한데...

뭐 전체 과정까지는 문제가 없는거 같은데, 나중에 실패하는거 보면 뭔가 망가트리고 있나?

테스트 조건은 다 통과하는데 close 할 때 실패함.

destroy에서 파일을 닫나? 

아?

뭔가 알거 같은데

이거 exit 하거나 munmap하는거 아니면 계속 열려야한다고 했으니까.

각 page들이 file을 복사해서 들고 있어야 함. (아니면 최소한 별도의 생명주기를 가져야함.)

왜냐면

```
open
mmap
close
... < 여기서도 접근 가능해야 함.
munmap
```

그러니까 로드 되고, lazy_init 때도 살아있고

종료 시에 파일에 써야하니까 그 때도 살아있어야 하고

mumap 시 풀려야 함.

근데 왜 안되는걸까????

-----------

이거는 AI 헬프 쓰기 뭐 모르겠다 ㄹㅇ 로그 보고 힌트라도 좀 알려달라하기.

이전처럼 시간 박아가면 될 수도 있는데 굳이굳이긴 함.


